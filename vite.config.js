import { defineConfig } from 'vite';

export default defineConfig({
  root: '.',
  // GitHub Pages: https://nurlykhan02.github.io/termmobalance-kp/
  base: '/termmobalance-kp/',
  build: {
    outDir: 'dist',
  },
});
